{
  "name": "@crack-attack/server",
  "version": "0.0.0",
  "private": true,
  "description": "Lobby + lockstep relay server for Crack Attack! (Node). WIP — Phase 4/5.",
  "license": "GPL-2.0-or-later",
  "type": "module",
  "main": "./dist/index.js",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "tsc -b",
    "bundle": "tsc -b && node scripts/bundle.mjs",
    "start": "node dist/main.js",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@crack-attack/core": "workspace:*",
    "@crack-attack/protocol": "workspace:*",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.28.2"
  }
}
