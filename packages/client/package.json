{
  "name": "@crack-attack/client",
  "version": "0.0.0",
  "private": true,
  "description": "Three.js browser client for Crack Attack! (Vite app). WIP — Phase 2.",
  "license": "GPL-2.0-or-later",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@crack-attack/core": "workspace:*",
    "@crack-attack/protocol": "workspace:*",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "vite": "^8.1.3"
  }
}
